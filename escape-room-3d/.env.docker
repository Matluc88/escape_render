# ==================================================
# Escape Room 3D - Docker Environment Configuration
# ==================================================
# Copia questo file come .env prima di avviare Docker Compose
# cp .env.docker .env

# --------------------------------------------------
# Database Configuration
# --------------------------------------------------
POSTGRES_USER=escape_user
POSTGRES_PASSWORD=escape_pass_CHANGE_ME_IN_PRODUCTION
POSTGRES_DB=escape_db

# Limiti risorse per Raspberry Pi (aumenta se hai più RAM)
DB_MEMORY_LIMIT=512M

# --------------------------------------------------
# Backend Configuration
# --------------------------------------------------
# Secret key per JWT/Sessions (CAMBIA IN PRODUZIONE!)
SECRET_KEY=your-secret-key-change-in-production-min-32-chars

# CORS Origins (aggiungi il tuo dominio o IP Raspberry Pi)
CORS_ORIGINS=http://localhost,http://localhost:80

# --------------------------------------------------
# MQTT Configuration
# --------------------------------------------------
MQTT_PORT=1883
MQTT_WS_PORT=9001
MQTT_MEMORY_LIMIT=128M

# --------------------------------------------------
# Frontend Configuration
# --------------------------------------------------
FRONTEND_PORT=80

# URL del backend (per produzione usa l'IP del Raspberry Pi)
# Esempio: VITE_API_URL=http://192.168.1.100:80
VITE_API_URL=http://localhost:80

# Backend e WebSocket URLs (configurato per Raspberry Pi)
VITE_BACKEND_URL=http://192.168.8.138:8000
VITE_WS_URL=ws://192.168.8.138:8000

# --------------------------------------------------
# Raspberry Pi Optimizations
# --------------------------------------------------
# Per Raspberry Pi 3: usa valori più bassi
# DB_MEMORY_LIMIT=256M
# MQTT_MEMORY_LIMIT=64M

# Per Raspberry Pi 4/5 con 4GB+ RAM: puoi aumentare
# DB_MEMORY_LIMIT=1G
# MQTT_MEMORY_LIMIT=256M

# --------------------------------------------------
# Development/Debug
# --------------------------------------------------
DEBUG=false
LOG_LEVEL=info
