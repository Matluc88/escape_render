# ==================================================
# Escape Room 3D - PRODUCTION Environment
# ==================================================
# ⚠️ IMPORTANTE: Cambia TUTTE le password e i secret!
# Usa: openssl rand -base64 32  per generare password sicure
# ==================================================

# --------------------------------------------------
# Database Configuration
# --------------------------------------------------
POSTGRES_USER=escape_user
POSTGRES_PASSWORD=CHANGE_ME_GENERATE_SECURE_PASSWORD
POSTGRES_DB=escape_db

# Limiti risorse - Ottimizzato per produzione
DB_MEMORY_LIMIT=1G

# --------------------------------------------------
# Backend Configuration
# --------------------------------------------------
# Genera con: openssl rand -base64 48
SECRET_KEY=CHANGE_ME_GENERATE_SECRET_KEY_MIN_48_CHARS

# CORS Origins - Sostituisci con il tuo dominio
CORS_ORIGINS=https://tuodominio.com,https://www.tuodominio.com

# --------------------------------------------------
# MQTT Configuration
# --------------------------------------------------
MQTT_PORT=1883
MQTT_WS_PORT=9001
MQTT_MEMORY_LIMIT=256M

# --------------------------------------------------
# Frontend Configuration
# --------------------------------------------------
# Porta pubblica (80 per HTTP, 443 per HTTPS)
FRONTEND_PORT=80

# URL relativi per Nginx proxy
VITE_BACKEND_URL=/api
VITE_WS_URL=/ws

# --------------------------------------------------
# Production Settings
# --------------------------------------------------
DEBUG=false
LOG_LEVEL=warning

# --------------------------------------------------
# Backup Configuration (opzionale)
# --------------------------------------------------
BACKUP_RETENTION_DAYS=7
BACKUP_PATH=/backup
